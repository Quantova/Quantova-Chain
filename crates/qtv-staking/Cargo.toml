[package]
name = "qtv-staking"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Stake accounting and reward rules for the node."

[dependencies]
qtv-codec = { path = "../qtv-codec" }
